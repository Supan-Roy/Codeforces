#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    int t;
    cin>>t;
    while(t--){
        string w;
        cin>>w;

        for(int i=0;i<w.size()-2;i++)
        {
                cout<<w[i];
        }
        cout<<"i"<<endl;
    }
    return 0;
}